# Review and Fix Workflow

Claude checks scope, acceptance criteria, data safety, accessibility, test evidence, and architecture fit. Material deviations require ADR before merge. Bugs become a new scoped task unless fix is trivially contained.
