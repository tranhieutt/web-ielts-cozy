// VOC-WEB-01/02 — Vocabulary dashboard route.
import { DeckCatalog } from '@/features/vocabulary/components/DeckCatalog';

export const metadata = { title: 'Từ vựng — IELTS Cozy' };

export default function VocabularyPage() {
  return <DeckCatalog />;
}
